{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "PRISMA_SCHEMA_DISABLE_ADVISORY_LOCK=1 npx prisma generate && npx prisma migrate deploy && next build",
  "outputDirectory": ".next",
  "regions": ["sin1"]
}